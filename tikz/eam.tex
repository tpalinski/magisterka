\begin{tikzpicture}[
    scale=0.65,
    transform shape,
    font=\small,
    block/.style={
        draw,
        rounded corners=2pt,
        thick,
        minimum width=2.4cm,
        minimum height=0.85cm,
        align=center
    },
    enc/.style={block, fill=blue!20},
    trans/.style={block, fill=purple!20},
    attn/.style={block, fill=orange!25},
    proj/.style={block, fill=green!20},
    pool/.style={block, fill=yellow!20},
    arrow/.style={->, thick}
]

% Text
\node[block] (text) {Tekst};

\node[enc, right=1.2cm of text] (txtenc)
    {Enkoder Tekstu / \\ Model Embeddingowy};

\transformerstack{right=1.2cm of txtenc}{txttr}

\node[attn, right=1.2cm of txttr] (txtattn)
    {\textit{Self-Attention}};


% Audio
\node[block, below=2cm of text] (audio)
    {Nagrania Mowy};

\node[enc, right=1.8cm of audio] (audenc)
    {Enkoder \\ (Model ASR)};

\transformerstack{below=1.85cm of txttr}{audtr}

\node[attn, right=1.2cm of audtr, below=2cm of txtattn] (audattn)
    {\textit{Self-Attention}};

% ============================================================
% SHARED HEAD
% ============================================================

% Place the shared pooling node between the two branches
\node[
    pool,
    right=1.5cm of txtattn,
    yshift=-1.5cm
] (pool) {Attention Pooling};

% Projection follows the pooling node
\node[
    proj,
    right=1.2cm of pool
] (proj) {Warstwa Liniowa \\ (Projekcja)};

% ============================================================
% TEXT ARROWS
% ============================================================

\draw[arrow] (text) -- (txtenc);
\draw[arrow] (txtenc) -- (txttr);
\draw[arrow] (txttr) -- (txtattn);


% ============================================================
% AUDIO ARROWS
% ============================================================

\draw[arrow] (audio) -- (audenc);
\draw[arrow] (audenc) -- (audtr);
\draw[arrow] (audtr) -- (audattn);

% ============================================================
% SHARED EMBEDDING SPACE
% ============================================================

\draw[arrow] (txtattn) -- (pool.north west);
\draw[arrow] (audattn) -- (pool.south west);
\draw[arrow] (pool) -- (proj);


% ============================================================
% BRANCH BOXES
% ============================================================

\node[
    draw,
    dashed,
    rounded corners,
    inner sep=15pt,
    fit=(text)(txtattn)(txtenc),
    label=above:Tekst
] {};

\node[
    draw,
    dashed,
    rounded corners,
    inner sep=15pt,
    fit=(audio)(audattn)(audenc),
    label=below:Mowa
] {};

\end{tikzpicture}
